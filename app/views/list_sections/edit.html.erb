<main class="container">
  <%= link_to sanitize("&larr; Back"), todo_lists_path %>

  <div class="header">
    <h1>Edit</h1>
  </div>

  <%= turbo_frame_tag nested_dom_id(@list_section.todo_list, @list_section) do %>
    <%= render "form", list_section: @list_section, todo_list: @list_section.todo_list %>
  <% end %>
</main>

