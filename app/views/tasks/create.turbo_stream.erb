<%= turbo_stream.update nested_dom_id(@task.todo_list, @task.list_section, Task.new) do %>
  <%= render partial: "new_task_link", locals: { list_section: @task.list_section } %>
<% end %>

<%= render_turbo_stream_flash_messages %>
